# Buffer media

Place each finished carousel in its own folder, using ordered file names such as:

`media/roman-empire/01_Hook.png`

Buffer can retrieve the public HTTPS image after GitHub Pages publishes the commit. Keep the files online until the TikTok post succeeds. The private Enlightiam Creator workspace remains the recommended upload method because it removes files automatically after seven days.
